var
        j, k is int;
        a, j real;
   begin
     read(j);
read(K);
     if (k <> 0)
        result := j/k
     else
        result := 0
write({Altura: });
in (altura);
soma := soma  altura;
_cont := _cont - 1;
end;
     out(result);
  end
